{% extends 'base.html.twig' %}

{% block title %}Team-mate - Modification du mot de passe
{% endblock %}

{% block body %}
	<div style="height:700px">
		<div class="container text-white">
			<h1 class="mt-4 text-center">Formulaire de modification de mot de passe
			</h1>

			{% for message in app.flashes('warning') %}
				<div class="alert alert-warning mt-4">
					{{ message }}
				</div>
			{% endfor %}

			{{ form(form) }}
			</div>
		</div>
	{% endblock %}
